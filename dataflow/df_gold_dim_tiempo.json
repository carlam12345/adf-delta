{
	"name": "df_gold_dim_tiempo",
	"properties": {
		"folder": {
			"name": "GOLD"
		},
		"type": "MappingDataFlow",
		"typeProperties": {
			"sources": [
				{
					"dataset": {
						"referenceName": "ds_sql_detalles_ventas",
						"type": "DatasetReference"
					},
					"name": "source1"
				}
			],
			"sinks": [
				{
					"dataset": {
						"referenceName": "ds_dim_tiempo",
						"type": "DatasetReference"
					},
					"name": "sink1"
				}
			],
			"transformations": [],
			"scriptLines": [
				"source(output(",
				"          sls_order_dt as date,",
				"          sls_ship_dt as date,",
				"          anio as integer,",
				"          mes as integer,",
				"          dia as integer",
				"     ),",
				"     allowSchemaDrift: true,",
				"     validateSchema: false,",
				"     isolationLevel: 'READ_UNCOMMITTED',",
				"     query: 'SELECT DISTINCT \\n    CAST(LEFT(CONVERT(VARCHAR(10), d.sls_order_dt), 8) AS DATE) AS fecha,\\n    YEAR(CAST(LEFT(CONVERT(VARCHAR(10), d.sls_order_dt), 8) AS DATE)) AS anio,\\n    MONTH(CAST(LEFT(CONVERT(VARCHAR(10), d.sls_order_dt), 8) AS DATE)) AS mes,\\n    DAY(CAST(LEFT(CONVERT(VARCHAR(10), d.sls_order_dt), 8) AS DATE)) AS dia\\nFROM SILVER.Detalle_ventas AS d\\nWHERE d.sls_order_dt IS NOT NULL;\\n',",
				"     format: 'query') ~> source1",
				"source1 sink(allowSchemaDrift: true,",
				"     validateSchema: false,",
				"     input(",
				"          id_tiempo as integer,",
				"          sls_order_dt as string,",
				"          sls_ship_dt as string",
				"     ),",
				"     deletable:false,",
				"     insertable:true,",
				"     updateable:false,",
				"     upsertable:false,",
				"     format: 'table',",
				"     skipDuplicateMapInputs: true,",
				"     skipDuplicateMapOutputs: true,",
				"     errorHandlingOption: 'stopOnFirstError',",
				"     mapColumn(",
				"          sls_order_dt,",
				"          sls_ship_dt",
				"     )) ~> sink1"
			]
		}
	}
}